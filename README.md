# tahpee

## Run locally
```bash
pip install -r requirements.txt
python main.py
```

## Build client
```bash
npm install

# before commit
npm run build

# comment code in vue.config.js before run
num run serve
```
